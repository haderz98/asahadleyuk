import { Box } from "@mui/material";
import React from "react";

const HeaderDivider = () => {
  return (
    <Box
      sx={{
        bgcolor: "#72E2AE",
        height: "5px",
        width: "100px",
        border: 1,
        borderRadius: "5px",
      }}
    />
  );
};

export default HeaderDivider;
